package com.company.changeassurance.domain.service;

/**
 * Marker package for pure domain services (no framework dependencies).
 * Concrete domain services are introduced with deterministic tools in Phase 2.
 */
public final class DomainServices {

    private DomainServices() {
    }
}
