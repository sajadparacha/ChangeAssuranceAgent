package com.company.changeassurance.adapter.out.persistence;

/**
 * Persistence adapters (JPA entities, Spring Data repositories) are introduced when
 * review lifecycle persistence is implemented (Phase 2/3). Domain models remain framework-free.
 */
public final class PersistencePackage {

    private PersistencePackage() {
    }
}
