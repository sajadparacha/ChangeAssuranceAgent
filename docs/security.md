# Security

- Upload extension/MIME/size/binary checks; path traversal blocked; server-generated filenames
- Credential pattern detection in SQL; SQL never executed
- Prompt-injection content treated as data
- API errors never return stack traces
- WebLogic 15.x / Java 17 WAR deployment supported
- Spring Security can be added later without redesigning domain
