# Demo Scenario

Artifacts in `demo-package/`.

Description claims package-body-only; SQL changes package spec/body, alters table, updates all rows.

Expected: mixed classification, many findings, clarification and/or `NO_GO_RECOMMENDED` / `INSUFFICIENT_INFORMATION`.
