# Frontend Architecture

Angular 19 standalone app under `frontend/`.

- Feature: `features/change-review` with models, HttpRepository, Facade (signals), pages
- No backend rules in Angular (no SQL parse, no recommendation calculation)
- Dev proxy: `proxy.conf.json` → backend `:8080`
